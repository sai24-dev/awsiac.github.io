name: "Terraform-Plan-Infra-Plan"
on:
  push:
    branches:
      - FeatureBranch
      - main

jobs:
  terraform:
    name: "Terraform"
    runs-on: ubuntu-latest
    env:	
      AWS_ACCESS_KEY_ID: "AKIAVCTRWKHRE6YM7XNO"
      AWS_SECRET_ACCESS_KEY: "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"
      AWS_REGION: 'ap-south-1'
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
      - name: Terraform Init
        id: init
        run: terraform init
      - name: Terraform Plan
        id: plan
        if: github.event_name == 'push'
        run: terraform plan -no-color	
		
